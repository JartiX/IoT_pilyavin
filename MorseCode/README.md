# Система связи на основе азбуки Морзе
---

## 1. Архитектура системы

Система реализует полудуплексную связь между двумя Arduino через GPIO. Код симметричный — оба МК используют один и тот же sketch.

### Структура данных

```
Передатчик:  ASCII → Morse Code → GPIO импульсы
Приёмник:    GPIO импульсы → Morse Code → ASCII
```

### Компоненты

- **Передатчик** (строки 207-263): генерирует последовательность HIGH/LOW на TX_PIN
- **Приёмник** (строки 265-407): измеряет длительности импульсов на RX_PIN
- **Кодировщик** (строки 133-144): ASCII → строка точек/тире
- **Декодировщик** (строки 146-155): строка точек/тире → ASCII

---

## 2. Аппаратная конфигурация

### Назначение пинов (строки 7-14)

| Пин | Функция | Обоснование |
|-----|---------|-------------|
| 2 | SENSOR_PIN | INT0 — прерывание для кнопки |
| 3 | RX_PIN | INT1 — прерывание для точного измерения |
| 5 | TX_PIN | Обычный GPIO для передачи |
| 8-10 | Shift Register | Управление 7-сегментом (экономия пинов) |
| 11-12 | LED_TX, LED_RX | Визуальная индикация |

**Почему прерывания для пинов 2 и 3:**
- Точность измерения импульсов критична (±10 мс)
- Main loop имеет задержки (Serial.print, display update)
- ISR гарантирует реакцию <10 мкс (строки 178-205)

**Почему TX_PIN не использует прерывания:**
- Передача контролируется state machine в loop()
- Точность не критична (допуск ±50 мс)
- Упрощает отладку

### Схема подключения

```
МК A: TX_PIN (5) ──────┐
                        ├──▶ GPIO линия
МК B: RX_PIN (3) ──────┘

МК B: TX_PIN (5) ──────┐
                        ├──▶ GPIO линия
МК A: RX_PIN (3) ──────┘
```

---

## 3. Временные параметры

### Константы (строки 17-29)

```cpp
DOT_DURATION = 200 ms    // Длительность точки
DASH_DURATION = 600 ms   // Длительность тире (3×DOT)
SYMBOL_GAP = 200 ms      // Пауза между точкой/тире
LETTER_GAP = 600 ms      // Пауза между буквами
WORD_GAP = 1400 ms       // Пауза между словами
```

### Диапазоны декодирования (строки 22-27)

```cpp
DOT:    120 ≤ t ≤ 350 ms
DASH:   400 ≤ t ≤ 900 ms
LETTER: пауза ≥ 400 ms
WORD:   пауза ≥ 1000 ms
```

**Обоснование широких диапазонов:**
- Ручной ввод: человек не может точно нажимать
- Raw-режим: передаются неисправленные сигналы
- Серая зона (350-400 мс): трактуется как DOT (строки 417, 421)

### Временная диаграмма для 'A' (.-):

```
Time:  0    200  400  600        1200
       ┌────┐    ┌──────────────┐
TX:    │ .  │    │      -       │
       └────┘    └──────────────┘
       [200][200][     600     ][200]
        DOT  GAP      DASH       GAP
```

---

## 4. Режимы работы

Система поддерживает 3 режима (enum Mode, строка 32):

### 4.1 AUTO Mode (строки 512-554, 585-587)

**Вход:** Serial Monitor → текст  
**Обработка:** программная генерация идеальных импульсов  
**Выход:** TX_PIN (строки 234-248)

**Как работает:**
```cpp
// Пользователь вводит "HELLO" + Enter
loop() { 
  if (Serial.available()) {           // строка 509
    autoMessage[] накапливает символы // строки 547-553
  }
  if (Enter pressed) {
    transmitInit(autoMessage)         // строка 544
  }
}

transmitUpdate() {                     // строки 229-263
  for (каждый символ) {
    encodeToMorse(symbol)              // строка 214, 253
    for (каждая точка/тире) {
      digitalWrite(TX_PIN, HIGH)       // строка 237
      delay_nonblocking(DOT/DASH)      // строки 241-242
      digitalWrite(TX_PIN, LOW)        // строка 243
    }
  }
}
```

### 4.2 MANUAL Mode (строки 409-455, 589-592)

**Вход:** кнопка на SENSOR_PIN  
**Обработка:** измерение длительности нажатия → классификация DOT/DASH  
**Выход:** TX_PIN

**Как работает:**
```cpp
// ISR фиксирует время нажатия/отпускания
buttonISR() {                          // строки 178-189
  if (pressed) buttonPressTime = now   // строка 182
  if (released) buttonReleased = true  // строка 187
}

// Main loop классифицирует
manualModeUpdate() {                   // строки 410-455
  duration = now - buttonPressTime     // строка 414
  
  if (120 ≤ duration ≤ 350)           // строка 417
    append('.')                        // строка 418
  else if (400 ≤ duration ≤ 900)      // строка 421
    append('-')                        // строка 422
  
  if (silence ≥ LETTER_GAP)           // строка 430
    decodeCharacter()                  // строка 434
  
  if (silence ≥ WORD_GAP)             // строка 446
    transmitInit(buffer)               // строка 451
}
```

**Особенность:** человек вводит код Морзе, система исправляет тайминги при передаче.

### 4.3 RAW Mode (строки 458-462, 580-582)

**Вход:** кнопка на SENSOR_PIN  
**Обработка:** ОТСУТСТВУЕТ  
**Выход:** прямая копия сигнала на TX_PIN

**Реализация:**
```cpp
void rawModeUpdate() {                 // строки 458-462
  bool state = digitalRead(SENSOR_PIN) // строка 459
  digitalWrite(TX_PIN, state)          // строка 460
  // Никакой обработки!
}
```

**Цель:** демонстрация устойчивости приёмника к несовершенным сигналам.

### Переключение режимов (строки 512-577)

**Умная логика команд:**
- Одиночный символ 'a', 'm', 'r' → смена режима
- Слова "arm", "mars" → обычный текст

```cpp
if (strlen(autoMessage) == 1) {       // строка 514
  char c = autoMessage[0]
  if (c == 'a') MODE_AUTO             // строка 517
  else if (c == 'm') MODE_MANUAL      // строка 524
  else if (c == 'r') MODE_RAW         // строка 532
}
else {
  transmitInit(autoMessage)           // строка 544 — передача текста
}
```

---

## 5. Конечные автоматы

### 5.1 Передатчик FSM (строки 96, 100)

**Состояния:** TX_IDLE, TX_SENDING, TX_COMPLETE

**Переходы:**
```
TX_IDLE ──[transmitInit]──▶ TX_SENDING ──[buffer empty]──▶ TX_COMPLETE ──[delay 100ms]──▶ TX_IDLE
          (строка 212)         │                              (строка 257)   (строки 604-606)
                               │
                         [transmitUpdate цикл]
                               │
                         ├── symbol == '.' : HIGH 200ms
                         ├── symbol == '-' : HIGH 600ms
                         └── pause : LOW
```

**Детали реализации:**
```cpp
// Инициализация (строки 208-227)
txState = TX_SENDING
encodeToMorse(txBuffer[0], currentMorseCode)  // первый символ
morseIndex = 0

// Обновление каждый loop() (строки 229-263)
if (morseIndex < strlen(currentMorseCode)) {
  symbol = currentMorseCode[morseIndex]
  digitalWrite(TX_PIN, HIGH)
  wait duration
  digitalWrite(TX_PIN, LOW)
  morseIndex++
}
else if (elapsed >= LETTER_GAP) {
  txIndex++
  if (txIndex < strlen(txBuffer)) {
    encodeToMorse(next char)
  }
  else txState = TX_COMPLETE
}
```

### 5.2 Приёмник FSM (строки 97, 108)

**Состояния:** RX_IDLE, RX_RECEIVING, RX_COMPLETE, RX_ERROR

**Переходы:**
```
RX_IDLE ──[first pulse]──▶ RX_RECEIVING ──┬──[timeout 3s]──▶ RX_COMPLETE ──[delay 2s]──▶ RX_IDLE
          (строка 290)       │              │   (строка 397)   (строки 599-601)
                             │              │
                        [receiveUpdate]     └──[error]──▶ RX_ERROR
                             │                  (не реализовано)
                             │
                      ├── newPulseComplete
                      │   └── classify DOT/DASH
                      │
                      └── silence >= LETTER_GAP
                          └── decodeFromMorse()
```

**Детали реализации:**

**ISR** (строки 191-205):
```cpp
void rxISR() {
  if (RX_PIN goes HIGH) {
    pulseStartTime = millis()         // строка 195
    signalActive = true               // строка 196
    LED_RX = HIGH                     // строка 197
  }
  else if (RX_PIN goes LOW) {
    pulseDuration = now - pulseStartTime  // строка 199
    newPulseComplete = true           // строка 201
    signalActive = false              // строка 200
    LED_RX = LOW                      // строка 203
  }
}
```

**Main loop** (строки 277-407):
```cpp
void receiveUpdate() {
  if (newPulseComplete) {              // строка 278
    duration = pulseDuration           // строка 280
    
    // Классификация
    if (120 ≤ duration ≤ 350)         // строка 296
      append('.')                      // строка 297
    else if (400 ≤ duration ≤ 900)    // строка 299
      append('-')                      // строка 300
  }
  
  // Детекция конца буквы
  silence = now - lastSignalTime       // строка 353
  if (silence ≥ LETTER_GAP_MIN) {     // строка 355
    decoded = decodeFromMorse(morse)   // строка 360
    displayChar(decoded)               // строка 368
    rxBuffer += decoded                // строки 363-367
  }
  
  // Детекция пробела
  if (silence ≥ WORD_GAP_MIN) {       // строка 370
    rxBuffer += ' '                    // строки 388-392
  }
  
  // Таймаут
  if (silence > 3000ms) {             // строка 397
    rxState = RX_COMPLETE              // строка 399
  }
}
```

### 5.3 Ручной ввод FSM (строки 117-122, 410-455)

**Реализация через ISR + Main loop:**

```
WAITING ──[button press]──▶ BUTTON_PRESSED ──[button release]──▶ CLASSIFY ──▶ DECODE ──▶ TRANSMIT
         (ISR строка 181)               (ISR строка 185)       (строка 414)  (строка 430)  (строка 446)
         buttonPressTime                buttonReleased=true    duration      silence       silence
         = millis()                                            check         ≥600ms        ≥1400ms
```

---

## 6. Протокол обмена данными

### Структура сообщения

Система использует **неявный протокол** без START/END маркеров:

```
[Молчание] → [Первый импульс = AUTO-START] → [Payload] → [Молчание 3s = END]
```

**Реализация AUTO-START** (строки 288-294):
```cpp
if (newPulseComplete && !autoStarted) {
  autoStarted = true                   // строка 290
  rxState = RX_RECEIVING               // строка 291
  Serial.println("AUTO: Receiving")    // строка 292
}
```

**Детекция конца** (строки 397-406):
```cpp
if (autoStarted && silence > AUTO_START_TIMEOUT) {  // 3000ms
  rxState = RX_COMPLETE                // строка 399
  Serial.print("RX: \"")               // строка 401
  Serial.print(rxBuffer)               // строка 402
}
```

### Обработка ошибок

**Неизвестный код Морзе** (строки 146-155):
```cpp
char decodeFromMorse(const char* morse) {
  // поиск в таблице...
  return '?';  // если не найден          // строка 154
}
```
Результат: на дисплее '?', но система продолжает работу.

**Переполнение буфера** (строки 363-367, 549-553):
```cpp
if (len < 63) {
  buffer[len] = ch
  buffer[len + 1] = '\0'
}
// Иначе: молчаливое игнорирование
```

**Прерванная передача** (строка 397):
```cpp
if (silence > 3000ms) {
  rxState = RX_COMPLETE  // Принудительное завершение
}
```

### Пример полного обмена "HI"

**Передатчик (МК A):**
```cpp
// Ввод: "hi" + Enter
transmitInit("HI")                     // строка 544

// H = ....
for i in [0..3]:
  TX_PIN = HIGH (200ms)
  TX_PIN = LOW (200ms)
delay(LETTER_GAP = 600ms)

// I = ..
for i in [0..1]:
  TX_PIN = HIGH (200ms)
  TX_PIN = LOW (200ms)

// Автоматический WORD_GAP от timeout
```

**Приёмник (МК B):**
```cpp
// RX_PIN детектирует 4 импульса по 200ms
append('.') × 4                        // строка 297
currentMorseReceived = "...."

// Молчание 600ms
decodeFromMorse("....") → 'H'         // строка 360
displayChar('H')                       // строка 368
rxBuffer = "H"

// RX_PIN детектирует 2 импульса
currentMorseReceived = ".."
decodeFromMorse("..") → 'I'
displayChar('I')
rxBuffer = "HI"

// Timeout 3s
Serial.print("RX: \"HI\"")            // строка 401-403
```

---

## 7. Обработка сигналов

### 7.1 Normal режим (AUTO/MANUAL)

**Передатчик:** программная коррекция таймингов
- AUTO: идеальные тайминги 200/600 мс (строки 17-18, 241-242)
- MANUAL: измеренные длительности → классификация → исправленная передача (строки 417-422, затем 451 → transmitInit)

**Приёмник:** декодирование с широкими допусками (строки 22-25, 296-309)

### 7.2 Raw режим

**Передатчик:** прямая копия SENSOR_PIN → TX_PIN (строки 459-460)
```cpp
digitalWrite(TX_PIN, digitalRead(SENSOR_PIN))
```

**Приёмник:** тот же алгоритм декодирования с допусками

**Почему работает:** диапазоны DOT (120-350) и DASH (400-900) достаточно широкие для декодирования неидеальных сигналов.

### 7.3 Устойчивость к шуму

**Реализованные меры:**
1. INPUT_PULLUP для кнопки (строка 479)
2. ISR для точного измерения (строки 191-205)
3. Фильтрация коротких импульсов (<50 мс): игнорируются (строка 304)
4. Адаптивная классификация:
   - t < DOT_MIN → DOT (строка 304)
   - DOT_MAX < t < DASH_MIN → DOT (строка 307)

---

## 8. Оптимизация памяти

### PROGMEM для констант (строки 35-93)

**Таблица Морзе:**
```cpp
const char morse_A[] PROGMEM = ".-";           // во Flash
const char* const morseTable[] PROGMEM = {...} // во Flash
```

**Экономия:** ~200 байт RAM перемещены во Flash

**Доступ:**
```cpp
strcpy_P(buffer, (char*)pgm_read_ptr(&morseTable[i]))  // строка 140
```

### F() макрос для строк (строки 223-226, 259, и др.)
```cpp
Serial.println(F("TX: Sending"))  // строка во Flash, не в RAM
```

**Экономия:** ~50 байт RAM

---

## 9. Ограничения системы

### 9.1 Ограничения по памяти

**RAM (2 КБ):**
- `txBuffer[64]`: 64 bytes (строка 101)
- `rxBuffer[64]`: 64 bytes (строка 109)
- `autoMessage[64]`: 64 bytes (строка 130)
- Stack + другие переменные: ~300 bytes

**Последствие:** максимальное сообщение 63 символа (строки 209, 364, 549)

### 9.2 Точность измерений

**Ограничено millis():**
- Разрешение: 1 мс
- Реальная точность: ±4 мс

**Влияние:** импульс 200 мс может измериться как 196-204 мс

**Смягчение:** широкие диапазоны (DOT: 120-350 вместо 180-220)

### 9.3 Скорость передачи

**Фактическая:**
- 1 точка = 200 мс → 5 DOT/sec
- Средняя буква: ~3 элемента → ~1.7 символа/сек
- С учётом пауз: ~1 символ/сек

**Пример:** "HELLO" (20 элементов) ≈ 15 секунд

### 9.4 Отсутствие коррекции ошибок

**Проблема:** неизвестный код → '?' (строка 154), но нет индикации ошибки передачи

**Не реализовано:**
- Контрольная сумма (CRC)
- Протокол подтверждения (ACK/NACK)
- Повторная передача

### 9.5 Полудуплекс

**Ограничение:** одновременная передача обоими МК → коллизия (не детектируется)

**Не реализовано:** CSMA/CD

### 9.6 Поддерживаемые символы

**Только:** A-Z, 0-9, пробел (строки 36-82)

**Не поддерживаются:** знаки препинания (!?.,;)

### 9.7 Дребезг кнопки

**Смягчение:** 
- INPUT_PULLUP (строка 479)
- Игнорирование импульсов <50 мс (строка 304)

**Не реализовано:** программный debounce с задержкой

---

## 10. Проектные решения

### 10.1 Прерывания для критичных операций

**Решение:** ISR только для SENSOR_PIN и RX_PIN (строки 493-494)

**Обоснование:**
- RX_PIN: точность измерения ±10 мс критична
- SENSOR_PIN: фиксация timestamp нажатия/отпускания
- TX_PIN: контролируется FSM, не требует микросекундной точности

### 10.2 Неблокирующая архитектура

**Реализация:** State Machines + millis() (строки 508-607)

```cpp
void loop() {
  // NO delay() anywhere!
  
  if (Serial.available()) processCommand()
  transmitUpdate()  // FSM step
  receiveUpdate()   // FSM step
  
  // Only 2 blocking delays for cleanup:
  if (rxState == RX_COMPLETE) delay(2000)     // строка 600
  if (txState == TX_COMPLETE) delay(100)      // строка 605
}
```

**Обоснование:** полудуплекс требует готовности к переключению TX/RX

### 10.3 Симметричный код

**Решение:** один sketch для обоих МК

**Обоснование:**
- Упрощение поддержки
- Возможность loopback тестирования на одном МК

### 10.4 Неявный протокол

**Решение:** детекция START/END через таймауты (строки 288-294, 397-406)

**Обоснование:**
- Простота реализации
- Естественность для азбуки Морзе
- Не требуется резервирование специальных последовательностей

**Ограничение:** нельзя передавать "бесконечный поток" без пауз

---

## 11. Демонстрация работы

### Ссылки

🔗 **Симуляция Tinkercad:** [https://www.tinkercad.com/things/jw8V2tMLZhx-cool-jaiks-maimu?sharecode=By_eyDQ8t1beANJ8IdfJGNBH8AkQCGiJs21HDkSbaoc]
🎥 **Видео:** [https://disk.yandex.ru/i/_vXjAaYmDOdTnQ][https://disk.yandex.ru/i/cSVSJRWXvXB-1g]  

### Контрольные точки

✅ Три режима работают (AUTO, MANUAL, RAW)  
✅ 7-segment отображает символы  
✅ Serial Monitor показывает процесс декодирования  
✅ LED мигают синхронно  
✅ Умная логика команд: 'a' → режим, "arm" → текст  

---

## 12. Заключение

Система успешно реализует:
- ✅ 3 режима работы (AUTO, MANUAL, RAW)
- ✅ Неблокирующую архитектуру (FSM + прерывания)
- ✅ Устойчивое декодирование (широкие допуски)
- ✅ Оптимизацию памяти (PROGMEM, F())
- ✅ Симметричную реализацию обоих МК

