# Детектирование нажатий на матричной клавиатуре с использованием прерываний таймера

## Аннотация

В данной работе представлена реализация системы считывания нажатий с матричной клавиатуры 3×3 на микроконтроллере Arduino с использованием прямой работы с регистрами и аппаратных таймеров. Разработанное решение обеспечивает точное детектирование одиночных и множественных одновременных нажатий, отслеживание длительности удержания кнопок и вывод информации только при изменении состояния клавиатуры.

## Оглавление

- [Введение](#введение)
- [Архитектура решения](#архитектура-решения)
- [Техническая реализация](#техническая-реализация)
  - [Работа с регистрами](#работа-с-регистрами)
  - [Настройка таймера](#настройка-таймера)
  - [Обработчик прерывания](#обработчик-прерывания)
  - [Детектирование изменений](#детектирование-изменений)
- [Дополнительная задача](#дополнительная-задача)
- [Результаты и демонстрация](#результаты-и-демонстрация)
- [Выводы](#выводы)

## Введение

Матричная клавиатура представляет собой эффективный способ подключения большого количества кнопок к микроконтроллеру с минимальным использованием выводов. Для клавиатуры размером N×M требуется всего N+M выводов вместо N×M при прямом подключении.

### Постановка задачи

Требовалось реализовать систему считывания нажатий с матричной клавиатуры 3×3 с выполнением следующих требований:

1. **Задача 0** Использование прямой работы с регистрами PIN и PORT вместо функций digitalWrite/digitalRead, а также реализация сканирования строк через прерывания таймера
2. **Задача 1**: Вывод информации только при изменении состояния кнопок
3. **Задача 2** Отслеживание времени начала нажатия и длительности удержания каждой кнопки
4. **Дополнительная задача**: Реализация на реальной макетной плате с клавиатурой 2×2 и защитными диодами

## Архитектура решения

### Принцип работы матричной клавиатуры

Клавиатура организована в виде матрицы:

```
       COL1  COL2  COL3
         |     |     |
ROW1 ----1-----2-----3----
         |     |     |
ROW2 ----4-----5-----6----
         |     |     |
ROW3 ----7-----8-----9----
         |     |     |
```

**Алгоритм сканирования:**
1. Все строки устанавливаются в HIGH (логическая 1)
2. Одна строка переключается в LOW (логическая 0)
3. Считываются состояния всех столбцов
4. Если кнопка нажата, соответствующий столбец будет в LOW
5. Процесс повторяется для следующей строки

### Структура программы

Программа состоит из следующих основных компонентов:

- **Обработчик прерывания таймера (ISR)** — выполняет циклическое сканирование строк
- **Функция process_button_states()** — анализирует изменения состояния кнопок
- **Функция report_pressed_buttons()** — выводит список нажатых кнопок
- **Главный цикл loop()** — обрабатывает результаты сканирования

## Техническая реализация

### Работа с регистрами

Вместо медленных функций digitalWrite() и digitalRead() используется прямое обращение к регистрам портов микроконтроллера:

```cpp
// Настройка направления пинов (DDR - Data Direction Register)
DDRD |= (1 << 2) | (1 << 3) | (1 << 4);  // PD2-PD4 как OUTPUT (строки)

DDRD &= ~((1 << 5) | (1 << 6) | (1 << 7));  // PD5-PD7 как INPUT (столбцы)
PORTD |= (1 << 5) | (1 << 6) | (1 << 7);     // Включение подтягивающих резисторов
```

**Преимущества прямой работы с регистрами:**
- Высокая скорость выполнения (операция за 1 такт вместо десятков)
- Возможность одновременной работы с несколькими пинами
- Детерминированное время выполнения

**Используемые регистры:**
- `DDRD` — регистр направления данных порта D
- `PORTD` — регистр вывода данных порта D
- `PIND` — регистр чтения данных порта D

### Настройка таймера

Для циклического сканирования строк используется 16-битный таймер Timer1 в режиме CTC (Clear Timer on Compare Match):

```cpp
void setup_timer() {
  cli();  // Отключение глобальных прерываний
  
  TCCR1A = 0;  // Сброс регистров таймера
  TCCR1B = 0;
  TCNT1 = 0;
  
  // Расчет значения для сравнения
  uint16_t compare_value = (F_CPU / (64UL * (1000UL / SCAN_INTERVAL_MS))) - 1;
  OCR1A = compare_value;
  
  TCCR1B |= (1 << WGM12);  // Режим CTC
  TCCR1B |= (1 << CS11) | (1 << CS10);  // Предделитель 64
  TIMSK1 |= (1 << OCIE1A);  // Разрешение прерывания по совпадению
  
  sei();  // Включение глобальных прерываний
}
```

**Расчет частоты прерываний:**

Для интервала сканирования 10 мс при тактовой частоте 16 МГц и предделителе 64:

```
OCR1A = (16,000,000 / (64 × 100)) - 1 = 2499
```

Таймер генерирует прерывание каждые 10 мс, что обеспечивает частоту сканирования 100 Гц для полного цикла по всем трем строкам (33.3 Гц на строку).

### Обработчик прерывания

Обработчик прерывания ISR(TIMER1_COMPA_vect) выполняет атомарное сканирование одной строки:

```cpp
ISR(TIMER1_COMPA_vect) {
  // 1. Установка всех строк в HIGH
  PORTD |= (1 << 2) | (1 << 3) | (1 << 4);
  
  // 2. Установка текущей строки в LOW
  PORTD &= ~row_masks[current_row];
  
  // 3. Задержка для стабилизации сигнала
  asm volatile("nop\n\t");
  asm volatile("nop\n\t");
  
  // 4. Чтение состояния всех столбцов за одну операцию
  uint8_t pin_state = PIND;
  
  // 5. Обработка каждого столбца
  for (uint8_t icol = 0; icol < NCOLS; icol++) {
    uint8_t btn_index = current_row * NCOLS + icol;
    current_state[btn_index] = !(pin_state & col_masks[icol]);
  }
  
  // 6. Переход к следующей строке
  current_row++;
  if (current_row >= NROWS) {
    current_row = 0;
    scan_complete = true;  // Флаг завершения полного цикла
  }
}
```

**Ключевые особенности:**
- Задержка `nop` обеспечивает стабилизацию сигнала после переключения строки
- Однократное чтение регистра PIND минимизирует время в прерывании
- Инверсия значения `!(pin_state & col_masks[icol])` преобразует LOW (нажато) в true

### Детектирование изменений

Функция process_button_states() реализует логику детектирования изменений состояния:

```cpp
void process_button_states() {
  bool state_changed = false;
  unsigned long current_time = millis();
  
  // Проверка изменений для каждой кнопки
  for (uint8_t i = 0; i < NROWS * NCOLS; i++) {
    // Обнаружение нажатия
    if (current_state[i] && !previous_state[i]) {
      press_start_time[i] = current_time;
      state_changed = true;
    }
    // Обнаружение отпускания
    else if (!current_state[i] && previous_state[i]) {
      unsigned long duration = current_time - press_start_time[i];
      Serial.print("Duration of pressing button number ");
      Serial.print(i + 1);
      Serial.print(" - ");
      Serial.print(duration);
      Serial.print(" ms, start an ");
      Serial.print(press_start_time[i]);
      Serial.println(" ms");
      state_changed = true;
    }
  }
  
  // Вывод текущего состояния только при изменении
  if (state_changed) {
    report_pressed_buttons();
    for (uint8_t i = 0; i < NROWS * NCOLS; i++) {
      previous_state[i] = current_state[i];
    }
  }
}
```

**Алгоритм работы:**
1. Сравнение текущего и предыдущего состояния каждой кнопки
2. При нажатии — сохранение времени начала нажатия
3. При отпускании — вычисление и вывод длительности удержания
4. Вывод списка нажатых кнопок только при изменении состояния
5. Обновление предыдущего состояния для следующей итерации

### Формат вывода

Программа выводит информацию в следующем формате:

**При нажатии/отпускании кнопок:**
```
Pressed buttons: 1, 3, 5
```

**При отпускании кнопки:**
```
Duration of pressing button number 3 - 4123 ms, start an 12987 ms
Pressed buttons: 1, 5
```

## Дополнительная задача

Для дополнительной задачи была собрана матричная клавиатура 2×2 на макетной плате с использованием защитных диодов.

### Необходимость диодов

При одновременном нажатии нескольких кнопок в матричной клавиатуре без диодов возникает проблема "фантомных нажатий". Например, при нажатии кнопок 1, 2 и 3 система может определить, что нажата и кнопка 4:

```
Нажаты:    1 --- 2
           |     |
           3 --- 4 (фантом!)
```

**Решение:** установка диодов последовательно с каждой кнопкой предотвращает образование паразитных токовых путей.

### Модификации кода

Для версии 2×2 изменены следующие параметры:

```cpp
#define NROWS 2
#define NCOLS 2

const uint8_t row_masks[NROWS] = {
  (1 << 2),  // PD2 - ROW1
  (1 << 3)   // PD3 - ROW2
};

const uint8_t col_masks[NCOLS] = {
  (1 << 5),  // PD5 - COL1
  (1 << 6)   // PD6 - COL2
};
```

Добавлено больше инструкций NOP для стабилизации на реальном железе:

```cpp
asm volatile("nop\n\t");
asm volatile("nop\n\t");
asm volatile("nop\n\t");
asm volatile("nop\n\t");
```

## Результаты и демонстрация

### Симуляция в Tinkercad

Рабочая симуляция схемы и кода доступна по ссылке:

🔗 **[Открыть симуляцию в Tinkercad](https://www.tinkercad.com/things/k2c6L5GVdXW-buttonmatrix?sharecode=p1VvFfqcrYA8nFS3lYclkJsZVUDzxsxxnKIdDaKWuQ8)**

Симуляция демонстрирует работу матричной клавиатуры 3×3 с возможностью одновременного нажатия нескольких кнопок. В Serial Monitor отображается информация о нажатых кнопках и длительности их удержания.

### Видео-демонстрация

Демонстрация работы на реальной макетной плате с клавиатурой 2×2:

🎥 **[Смотреть видео-демонстрацию](https://drive.google.com/file/d/1pvMTMJMlGYsoRx3E6vUwUba7wF-Nu3a0/view?usp=drivesdk)**

Видео показывает:
- Работу с одиночными нажатиями
- Одновременное нажатие нескольких кнопок
- Корректное отображение длительности удержания
- Отсутствие ложных срабатываний благодаря защитным диодам

### Особенности реализации

**Что работает:**
- ✅ Детектирование одиночных нажатий
- ✅ Детектирование множественных одновременных нажатий
- ✅ Точное измерение времени начала нажатия и длительности
- ✅ Вывод информации только при изменении состояния
- ✅ Защита от фантомных нажатий с помощью диодов

## Выводы

В результате выполнения работы была реализована эффективная система считывания нажатий с матричной клавиатуры с использованием современных подходов программирования микроконтроллеров:

1. **Оптимизация производительности:** Прямая работа с регистрами обеспечивает минимальное время отклика и детерминированное поведение системы.

2. **Эффективное использование ресурсов:** Применение аппаратных таймеров освобождает процессор для других задач между прерываниями.

3. **Точность детектирования:** Алгоритм сравнения состояний позволяет надежно определять моменты нажатия и отпускания кнопок.

4. **Практическая применимость:** Система может быть использована в реальных проектах, требующих ввода с клавиатуры, например, в системах управления, кодовых замках, калькуляторах.

5. **Расширяемость:** Код легко масштабируется на клавиатуры других размеров (4×4, 4×3 и т.д.) путем изменения констант и добавления соответствующих масок.
